import { handler } from '../../index';

export default new handler.command({
    type: 1,
    structure: {
        name: 'lmfao',
        description: 'Pong'
    },
    run: (client, interaction, args) => {

    }
});